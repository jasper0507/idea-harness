# 冒烟测试

静态检查已脚本化：`bash scripts/verify.sh`（POSIX / CI）或 `pwsh -File scripts/verify.ps1`（Windows）。

以下为手动行为测试用例。

---

## Case 1：模糊想法 → gathering

输入：`我想做一个学习管理网站。`

- 只展示 `## 已确认` 和 `## 追问`。
- 不展示任何内部术语（Status、Harness Gate 等）。
- 问题聚焦具体学习痛点，不问技术栈。
- 给 2–4 个有意义选项 + 面向最小第一版的推荐。

## Case 2："都要" → blocked

输入：`这些功能都要。`

- 构造代价场景，只问 V1 先做哪一个。
- 不输出需求简报或执行 Prompt。

## Case 3："你决定" → blocked

输入：`你决定吧。`

- 给推荐 + 用用户场景解释 + 要求确认。
- 确认前不能进入 Ready。

## Case 4：门槛全部但精确度不足 → needs-precision

已确认事实全部模糊（"记录工具""自己用""平时用""记录""保存""不做登录""能记录"）。

- 不进入 Ready。
- 追问精确度缺口，使用精确度问法（假设暴露、场景走查、对比锚定）。

## Case 5：Ready 默认只输出需求简报

所有门槛 + 精确度检查通过。

- 展示 `## 需求简报`。
- 每条不做事项附带原因。
- 不展示 Harness Gate 或执行 Prompt。

## Case 6：明确要求时才输出执行 Prompt

输入：`现在生成可以交给 Codex 的执行 Prompt。`

- 先展示需求简报，再展示执行 Prompt。
- 行为契约风格（描述行为，不描述 UI 元素）。
- 不得实现附带原因。
- 除非用户确认技术栈，否则不选择技术栈。

## Case 7：隐藏默认值拦截

输入：`我想做个记账工具，能记录每天花了多少钱。`

- 不假设账号、云同步、数据库、图表等。
- 如果用户说"下次打开还在"，追问浏览器本地还是云端。

## Case 8：冲突答案 → blocked

用户先说"不需要登录"，后说"每个人登录后看自己的内容"。

- 把两句原话并排引出来。
- 只问第一版到底是否需要登录。

## Case 9：术语一致性

用户分别说了"记录学习内容"和"看学习进度"。

- 追问"内容"和"进度"是否指同一件事。

## Case 10：场景走查

七个门槛都有表面回答，但使用场景有一步未确认。

- 不进入 Ready。
- 指出场景中的断裂步骤。

## Case 11：原型逃生口

连续 3 轮追问后用户对视觉或流程仍然模糊。

- 不继续文字追问。
- 建议低保真原型（UI 或流程）。

---

## 通过标准

- SKILL.md 不超过 120 行，核心流程完整。
- 四个支撑文件各自独立可读。
- 四个状态行为可区分。
- 精确度检查能拦住"表面完整但不够精确"的需求。
- 不做事项附带原因。
- 执行 Prompt 使用行为契约风格。
- 用户可见输出不暴露内部术语。
- 默认输出不发明登录、后端、支付、AI、统计、通知、分享、同步或技术栈。
